# Gói inference

Module ML được tách thành các lớp preprocess, feature, predict và postprocess để dễ tích hợp vào Spark hoặc service inference riêng.

Trạng thái hiện tại: khung giữ chỗ. Giai đoạn tiếp theo cần thay predictor demo bằng baseline/solution CAFA-6 đã chọn, kèm `model_version`, `feature_version` và benchmark latency.
