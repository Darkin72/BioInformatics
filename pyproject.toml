[project]
name = "bioinformatics-realtime-protein"
version = "0.1.0"
description = "Minimal skeleton for a realtime protein function prediction platform."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
